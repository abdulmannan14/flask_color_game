from .input import (
    generateCode,
    check_guess, createCode, 
)

from .giveHint import (
    giveHint, createS,
    removeS, CountFrequency,
    findMiniMax, getRandomColor, createS,
    giveHint
)

